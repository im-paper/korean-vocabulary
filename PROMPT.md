# 페이퍼 어휘장 — 서비스 기획서 겸 개발 프롬프트

> 이 문서는 앱을 만들 때의 기준점입니다.
> 새 기능을 붙이거나 방향이 흔들릴 때 여기로 돌아와서 판단합니다.
>
> **개정 2026-08-24** — 산출(내 문장) 축을 추가하고, 미결정 항목을 확정했습니다.

---

## 0. 이번 개정에서 바뀐 것

| 바뀐 곳 | 내용 |
|---|---|
| 1·2절 | 한 줄 정의에 **산출**을 넣었다. "익혀두는 곳" → "내 문장이 될 때까지" |
| 3절 | 원칙 하나 추가 — **쓰기로 끝난다** |
| 5.3 | 되새김 피드가 소비 전용에서 **산출이 일어나는 자리**로 바뀌었다 |
| 5.5 | 사전 → **찾기**. 표제어 검색 외에 "느낌으로 찾기" 입구를 추가 |
| 6.3 | 교차 노출 대상에 **내가 쓴 문장**을 포함 |
| 7절 | 쓴 단어는 복습 간격을 한 단계 건너뛴다 |
| 8절 | 학습 기록 데이터 형태를 확정. 단어 고정 ID 규칙 추가 |
| 10절 | 1단계를 "로그인 제거"에서 **"저장소 어댑터 분리"**로 교체 (기존 코드 삭제 없이) |
| 12절 | 미결정이던 네 가지를 확정 |

**왜 바꿨나 —** 기존 기획은 전부 *입력*이었다. 카드 넘기기, 되새김 피드, 4지선다, 사전 검색.
사용자가 무언가를 *내놓는* 순간이 한 번도 없었다.
그런데 입력만 놓고 보면 검색과 Anki가 이미 하고 있고, 그쪽에서는 이길 수 없다.

---

## 1. 한 줄 정의

**책에서 만난 단어가 내 문장이 될 때까지 따라오는 곳.**

세 부분이 각각 일을 한다.
`책에서 만난` = 남의 단어 리스트가 아니라 내 맥락 / `따라오는` = 한 번 보고 끝나지 않는다 / `내 문장이 될 때까지` = 알아보는 게 아니라 쓸 수 있는 게 끝점

---

## 2. 해결하려는 문제

### 2.1 세 층

책을 읽다가 모르는 단어가 나오면 흐름이 끊긴다. 찾아보면 그 순간엔 알겠는데,
책을 덮으면 잊는다. 다음에 같은 단어를 만나도 또 모른다.
그리고 몇 번을 다시 봐도, 막상 내가 글을 쓸 땐 그 단어가 떠오르지 않는다.

| 층 | 문제 | 장치 |
|---|---|---|
| 1 | 읽다가 막힌다 | **읽기 전에 미리** — 섹션 예습 |
| 2 | 보고 나서 잊는다 | **잊기 전에 다시** — 망각곡선 + 되새김 피드 |
| 3 | 알아는 보는데 못 쓴다 | **쓸 수 있을 때까지** — 내 문장 남기기 |

3층이 이번에 추가된 부분이고, 여기가 제일 깊다.

### 2.2 검색이 경쟁자가 아닌 이유

검색이 해결하는 건 **이해**지 **산출**이 아니다.

- 사전은 `단어 → 뜻` 한 방향만 잘한다. 모르는 단어를 만났을 때는 검색으로 끝이고, 여기선 앱이 이길 수 없다
- 사람이 실제로 막히는 건 `뜻 → 단어` 방향이다. "억지로 참는데 겉으론 태연한 척하는 그 단어"는 검색창에 넣을 수가 없다
- 말하거나 쓰는 순간엔 아무도 검색하지 않는다. 정확히는 못 한다. 검색어 자체를 모르니까

**읽을 때 이해하는 단어**는 넓고 **쓸 때 꺼내 쓰는 단어**는 좁다.
그 간격이 답답함의 정체이고, 책을 많이 읽는 사람일수록 위쪽이 길어져서 간격을 더 크게 느낀다.
이 앱이 사는 자리가 거기다.

---

## 3. 핵심 원칙

설계 판단이 필요할 때 이 순서로 결정한다.

| 원칙 | 의미 |
|---|---|
| **읽는 것이 먼저** | 관리(추가·삭제·설정)보다 읽고 익히는 화면이 항상 중심 |
| **마찰은 최소로** | 뭘 누를지 고민하게 만들지 않는다. 열면 바로 보인다 |
| **짧게 여러 번** | 한 번에 오래 붙잡지 않는다. 30초씩 자주 |
| **쓰기로 끝난다** | 모든 흐름의 종착점은 내가 한 줄 쓰는 것. 단, 강요하지 않는다 |
| **성취가 보이게** | 쌓은 만큼 눈에 보여야 계속하게 된다 |
| **뜻은 사전 기준** | 정의는 국립국어원 표준국어대사전 원문을 벗어나지 않는다 |

**애매할 때의 최종 질문:** *"이게 사용자가 자기 문장을 쓰게 만드는가?"*
아니면 붙이지 않는다. 이 질문 하나로 대부분의 기능 논쟁이 정리된다.

---

## 4. 사용자 흐름

### 주 흐름 — 책 읽기 전 준비
```
책 선택 → 오늘 읽을 섹션 선택 → 어휘 훑기 → 퀴즈로 확인 → 책 읽으러 감
                                                              ↓
                                              (읽고 나서, 내킬 때) 한 줄 남기기
```

### 보조 흐름 1 — 책을 안 읽는 날
```
일일 어휘 10개 (책과 무관한 문학 어휘)
```

### 보조 흐름 2 — 읽다가 막혔을 때
```
찾기 → ┬ 단어를 안다  → 표제어 검색 → 뜻 확인 → 내 단어장에 담기
       └ 단어를 모른다 → 느낌으로 찾기 → 후보 단어 → 뜻 확인 → 내 단어장에 담기
```

### 보조 흐름 3 — 틈날 때 (가장 자주 일어나야 하는 흐름)
```
앱 열기 → 되새김 피드 스크롤 → 예전 단어가 예문과 함께 흘러감
                              → 그중 한둘에 "한 줄 쓰기" 칸이 열려 있다
                              → 쓰거나, 그냥 넘긴다
```

---

## 5. 화면별 기능

### 5.1 홈 — 첫 화면

세 덩어리로 구성한다. 위에서부터:

**① 오늘의 되새김**
- 앱을 열면 아무것도 누르지 않아도 보이는 영역
- 예전에 배운 단어 1~3개가 예문과 함께 표시
- 매번 다른 단어. 복습할 때가 된 단어를 우선으로
- **내가 쓴 문장이 있는 단어면 앱 예문 대신 내 문장을 보여준다**

**② 내 책꽂이**
- 처음엔 텅 빈 책장
- 섹션을 하나 끝낼 때마다 책등이 하나씩 꽂힌다
- 책 표지 이미지는 쓰지 않는다 (저작권). 색깔 있는 책등 + 제목 텍스트
- 한 권을 다 끝내면 얇은 책등들이 두꺼운 한 권으로 합쳐진다
- 책장 아래 한 줄: `배운 단어 128 · 내가 쓴 문장 31`

**③ 달력**
- 공부한 날에 표시
- 연속 일수(스트릭) 표시 — 다만 **끊겨도 벌하지 않는다.** 숫자만 조용히 리셋
- 그날 배운 단어 수 또는 책 제목을 작게

### 5.2 학습 — 어휘 익히기

```
책 고르기 → 섹션 고르기 → 카드 넘기며 보기
```

- 카드 한 장 = 단어 하나 (단어 / 한자 / 뜻 / 예문 / 유의어)
- 좌우로 넘김. 다 넘기면 "퀴즈로 확인하기"
- 이미 아는 단어는 "알아요" 표시해서 이후 출제에서 제외
- 사전 원문 예문은 접어두고, 탭하면 펼쳐진다

### 5.3 되새김 — 그냥 읽는 화면 *(개정 핵심)*

기존 기획에서 "가장 자주 일어나야 하는 흐름"으로 지목한 화면인데,
지금 설계로는 순수 소비다. **산출을 심을 자리가 여기다.** 마찰이 가장 낮기 때문이다.

- **퀴즈가 아니다.** 정답을 요구하지 않는다
- 지금까지 배운 단어들의 예문이 흘러가는 피드
- 단어는 강조 표시. 탭하면 뜻이 펼쳐짐
- **하루 분량은 5~7장으로 끝이 있다.** 무한 스크롤이 아니다 (→ 11절)
- 다 보면 "오늘 되새김 끝" 표시

**한 줄 쓰기 (여기가 추가분)**
- 하루 5~7장 중 **1~2장에만** 입력칸이 열린다. 전부에 열면 부담이 된다
- 열리는 대상: 복습 간격이 무르익었고 아직 내 문장이 없는 단어
- 입력칸은 한 줄. 플레이스홀더는 `이 단어로 한 줄 —`
- **채점하지 않는다.** 맞고 틀림이 없다. 저장하고 넘어갈 뿐
- 건너뛰기가 기본값이다. 비우고 넘겨도 아무 일도 일어나지 않는다
- 쓰고 나면 즉시 보상: *"이제 이 단어는 당신 문장으로 돌아옵니다"*

**보상 구조 —** 내가 쓴 문장은 그 단어가 다음에 돌아올 때 앱 예문을 대체한다.
내가 만든 게 앱의 콘텐츠가 되는 구조라서, 쓸수록 앱이 내 것이 된다.

### 5.4 퀴즈 — 30초 게임

- **기본 방식: 뜻 고르기 4지선다** (한 문제 3초, 10문제 30초)
- 출제 대상: 오늘 배운 것 + 복습할 때가 된 것을 섞어서
- 틀려도 막히지 않는다. 정답과 예문을 바로 보여주고 다음으로
- 타이핑 방식은 기본으로 쓰지 않는다 (느리고, 한 글자 틀려도 오답이라 지친다)

**오답 보기 생성 규칙** (구현할 때 반드시 지킬 것)
1. 같은 `level`의 다른 단어 뜻에서 뽑는다
2. 같은 책·섹션 안에서 먼저 찾고, 모자라면 전체에서 채운다
3. **뜻의 글자 수를 비슷하게 맞춘다.** 정답만 길면 읽지 않고도 맞힌다
4. 유의어 관계인 단어는 오답 보기로 쓰지 않는다 (정답이 둘이 된다)

**나중에 추가할 방식** (우선순위 순)
1. O/X 빠른 판단 — 한 문제 2초
2. 예문 빈칸 채우기 (객관식 보기 제공)
3. 단어↔뜻 짝 맞추기
4. 플래시카드 (알아요/몰라요)

### 5.5 찾기 — 검색 *(사전 → 찾기로 개명)*

입구가 둘이다. 기존 기획엔 위쪽 하나뿐이었는데, 그러면 **단어를 이미 아는 사람만 쓸 수 있다.**
가장 강한 동기(표현 결핍)는 단어를 모르는 상태에서 시작하므로 아래쪽 입구가 필요하다.

**① 표제어로 찾기**
- 국립국어원 표준국어대사전 Open API
- 결과에서 "내 단어장에 담기"

**② 느낌으로 찾기** *(추가분)*
- 상태를 문장으로 적으면 후보 단어를 준다
  - 입력: `억지로 참는데 겉으론 태연한 척`
  - 출력: `짐짓 · 자제하다 · 태연자약 · 위악` …
- 표준국어대사전은 이 질의를 못 받으므로 LLM을 쓴다
- **나온 단어는 반드시 표제어 검색으로 되돌려 검증한다.** 뜻과 출처는 사전에서만 가져온다
  → 탐색은 모델이, 신뢰는 사전이 담당한다
- 여기서 고른 단어는 바로 단어장에 들어간다 (등록 마찰 0)

단어 추가 기능은 전부 이 화면 안에 둔다 (다른 화면에 노출하지 않음).

### 5.6 문장 노트 — 내가 쓴 것들 *(신규, 작게)*

- 내가 쓴 문장을 시간순으로 모아 보는 화면
- 단어별 / 책별로 묶어서 볼 수 있다
- 편집·삭제 가능. 첨삭이나 평가는 하지 않는다
- 홈의 `내가 쓴 문장 31`을 누르면 여기로 온다
- 우선순위는 낮다. 문장이 쌓인 다음에 의미가 생긴다

### 5.7 밑줄 — 책에서 옮겨 적은 문장 *(신규)*

출판사 달력처럼 **책을 대표하는 문장을 카드로 보여 주는** 기능이 필요했다.
다만 앱이 명대사를 *주는* 형태로는 만들 수 없었다. 두 군데에서 막힌다.

- **저작권** — 11절에서 번역본 인용을 이미 금지했다. 원문 저작권이 풀린 책도
  번역은 대개 안 풀려 있다 (인간실격은 원문 만료, 데미안·이방인은 원문도 보호 중).
  한 문장씩 출처를 달아도, 그 문장 자체가 앱의 콘텐츠가 되면 인용으로 방어하기 어렵다
- **원칙** — 3절의 최종 질문("이게 사용자가 자기 문장을 쓰게 만드는가")에 걸린다.
  명대사를 띄워 주는 것은 순수 소비다. 5.3에서 되새김 피드를 소비 전용에서
  꺼내 온 방향과 반대로 간다

그래서 **주지 않고 받는다.** 읽다가 걸린 문장을 사용자가 한 줄 옮겨 적고,
그것이 나중에 카드로 돌아온다.

- 본인이 본인 기기에 옮겨 적는 것이고 남에게 보이지 않는다 (6.6과 같음) → 저작권 문제 없음
- 옮겨 적기는 소비가 아니라 **산출**이다. 지어내기보다 마찰이 훨씬 낮아서
  '한 줄 쓰기'로 가는 계단이 된다
- **핵심** — 밑줄 안에 이미 배운 단어가 있으면 그 단어를 표시하고 다시 만난 것으로 센다.
  6.3의 예문 교차 노출을 사용자가 직접 만드는 셈이고, 콘텐츠 제작 부담이 0이다

**받는 자리 · 보여 주는 자리**

| 자리 | 하는 일 |
|---|---|
| 묶음 끝 | 단어를 다 넘긴 직후, 책을 읽으러 가기 직전에 묻는다 |
| 되새김 피드 **맨 끝** | 오늘의 밑줄 한 장. 앞에 두면 오늘 몫을 밀어내므로 끝에 둔다 |
| 책 결산 | 이 책의 밑줄이 모인다. 편집은 없고 삭제만 된다 |

**규칙**

- 채점하지 않는다. 맞춤법도 보지 않는다 (6.6과 같음)
- 길이는 300자. 옮겨 적는 것이라 내 문장보다 조금 넉넉하게
- 하루 한 장만 보여 준다. 매일 새 문장이 무한정 공급되는 형태로 만들지 않는다 (11절)
- **간격 건너뛰기는 주지 않는다.** 밑줄에서 만난 단어는 `lastSeenAt`만 갱신한다 —
  베끼기는 지어내기만큼 강한 인출이 아니다 (7절)
- 아직 한 줄도 없으면 빈자리 대신 무엇을 하는 자리인지만 조용히 적는다

---

## 6. 콘텐츠 규칙

### 6.1 뜻 (정의)

- **반드시 국립국어원 표준국어대사전 원문 그대로**
- 다의어는 그 책의 문맥에 맞는 뜻을 고른다
- 임의로 쉽게 풀어쓰지 않는다

### 6.2 예문

**기본은 책 맥락 예문을 쓴다.**

사전 예문은 짧고 건조해서 장면이 안 그려진다. 기억에 남으려면 상황이 보여야 한다.

```
사전 예문   "익살 섞인 웃음."                    ← 상황이 안 그려짐
책 맥락     "그는 익살로 사람들을 웃겼지만        ← 장면이 보임
             속으로는 늘 겁에 질려 있었다."
```

**규칙:**
- 책 맥락 예문은 **새로 쓴다.** 번역본 문장을 그대로 옮기지 않는다 (번역가 저작권)
- 책의 상황과 분위기만 반영한다
- "학습용 예문"이라고 표시한다. 사전 원문인 척하지 않는다
- 사전 원문 예문도 함께 보관하고, 접어둔 상태로 두어 원하면 펼쳐볼 수 있게 한다

### 6.3 예문 교차 노출 (중요)

새 단어의 예문 안에 **이미 배운 단어를 섞어서** 쓴다.

```
새 단어: 환멸
예문: "익살도 위선도 다 소용없다는 걸 알았을 때
      남은 건 환멸뿐이었다."
       ↑     ↑
      앞서 배운 단어가 자연스럽게 재등장
```

복습이라는 느낌 없이 계속 마주치게 만드는 장치다.

**여기에 내 문장을 포함시킨다.** 내가 쓴 문장 안에 이미 배운 단어가 들어 있으면
그 단어의 복습으로도 계산한다. 남이 쓴 어떤 예문보다 강하게 붙고, 콘텐츠 제작 부담도 준다.

### 6.4 난이도

| 단계 | 기준 | 예시 (인간실격) |
|---|---|---|
| 1단계 | 뜻은 알지만 정확히는 모호한 말 | 고독, 절망, 위선, 경멸 |
| **2단계** | 책에서 자주 보이나 일상에선 잘 안 씀 | 익살, 비굴, 처세, 환멸, 냉소 |
| 3단계 | 읽다가 실제로 걸리는 한자어·문어체 | 염세, 자조, 방종, 위악, 기만, 굴종, 유폐, 전락 |

- **2단계부터 시작한다**
- 앱에서 단계를 올리고 내릴 수 있다
- "알아요" 표시한 단어는 자동으로 제외되어 개인 수준에 맞춰진다

### 6.5 섹션 구조

책은 통으로 다루지 않고 **챕터/섹션 단위로 쪼갠다.**

```
인간실격
├─ 서문           (8개)
├─ 첫 번째 수기    (12개)
├─ 두 번째 수기    (15개)
├─ 세 번째 수기    (14개)
└─ 후기           (6개)
```

**선택 방식 3가지를 모두 지원한다:**
- 특정 섹션까지만 (오늘 거기까지 읽을 예정)
- 한 권 통으로 (10개씩 나눠서 여러 날에 걸쳐)
- 그냥 10개 (아직 안 배운 것 중 자동 선택)

### 6.6 내 문장 규칙

- **채점하지 않는다.** 맞춤법도, 어법도, 단어를 제대로 썼는지도 평가하지 않는다
- 첨삭·교정·AI 피드백을 붙이지 않는다. 평가가 붙는 순간 쓰기가 시험이 되고 사람은 안 쓴다
- 길이 제한은 두되 넉넉하게 (200자). 한 줄이 기본이지만 더 쓰고 싶으면 막지 않는다
- 한 단어에 여러 문장을 쓸 수 있다. 나중에 쓴 것이 먼저 보인다
- 삭제는 언제든 가능. 남에게 보이지 않는다 (공유 기능은 지금 없다)

---

## 7. 학습 알고리즘 — 망각곡선 복습

배운 날 기준으로 자동 재등장시킨다.

```
1일 후 → 3일 후 → 7일 후 → 30일 후 → 졸업
```

- 맞히면 다음 간격으로 넘어간다
- 틀리면 간격이 짧아진다 (1일 후로 되돌림)
- **그 단어로 문장을 쓰면 간격을 한 단계 건너뛴다** (1일 → 7일)
  → 직접 만들어 쓰는 인출이 보기에서 고르는 인출보다 훨씬 강하기 때문
- 사용자가 "뭘 복습할지" 고민하지 않게 한다. 앱이 알아서 꺼내온다
- 복습 대상은 홈의 "오늘의 되새김", 되새김 피드, 퀴즈 출제에 자동 반영
- 30일을 통과하면 졸업 처리하되, 되새김 피드에는 아주 낮은 확률로 계속 섞는다

---

## 8. 데이터 구조

### 8.1 저장 위치

| 무엇 | 어디에 | 이유 |
|---|---|---|
| 단어 콘텐츠 (뜻·예문·유의어·난이도) | 프로젝트 파일 | 잃으면 안 되는 자산 |
| 학습 기록 (진도·복습 일정·정답률·알아요) | 브라우저 저장 | 로그인 없이 바로 사용 |
| **내가 쓴 문장** | 브라우저 저장 | 위와 같음. 단 **가장 먼저 클라우드로 옮길 대상** |

**로그인은 지금 요구하지 않는다.** 전체 흐름을 확인하는 데 방해가 된다.
다만 코드를 지우지는 않는다 (→ 9.2 저장소 어댑터).

문장은 사용자가 직접 만든 유일한 자산이라, 기기를 바꾸면 잃는다는 게 가장 아프다.
동기화를 붙일 때 **문장부터** 붙인다.

### 8.2 단어 데이터 형태

```js
{
  id:          "ningen-1-jajo",              // 고정 ID. 아래 규칙 참조
  word:        "자조",
  hanja:       "自嘲",
  meaning:     "자기를 비웃음.",              // 표준국어대사전 원문
  example:     "돌아서면 {{자조}}만 깊어졌다.", // 학습용 예문, {{}}는 퀴즈 빈칸
  dictExample: "그는 자조 섞인 웃음을 지었다.", // 사전 원문 예문 (접어둠)
  synonyms:    ["자기비하", "자기멸시"],
  level:       2,                            // 난이도 1~3
  sourceUrl:   "https://stdict.korean.go.kr/..."
}
```

**고정 ID 규칙 (중요)**
- 학습 기록과 내 문장이 이 ID로 단어를 가리킨다. **한 번 정하면 바꾸지 않는다**
- 형식: `{책슬러그}-{섹션번호}-{단어로마자}` (예: `ningen-1-jajo`)
- 일일 어휘는 `daily-{연번}`
- 배열 인덱스나 배열 순서를 ID로 쓰지 말 것. 단어를 하나 끼워 넣는 순간 모든 기록이 어긋난다
- 나중에 Supabase로 옮길 때 이 ID를 `words.slug`로 그대로 들고 간다

### 8.3 책 데이터 형태

```js
{
  slug:    "ningen",
  book:    "인간실격",
  author:  "다자이 오사무",
  sections: [
    { name: "서문",        words: [...] },
    { name: "첫 번째 수기", words: [...] }
  ]
}
```

### 8.4 학습 기록 형태 (localStorage)

```js
// paper.progress
{
  "ningen-1-jajo": {
    known:      false,          // "알아요" 표시
    interval:   3,              // 현재 복습 간격 (일). 0이면 아직 안 배움
    dueAt:      "2026-08-27",   // 다음 등장일
    attempts:   4,
    correct:    3,
    lastSeenAt: "2026-08-24"
  }
}

// paper.sentences
{
  "ningen-1-jajo": [
    { text: "돌아서고 나서야 자조가 올라왔다.", createdAt: "2026-08-24T21:03:00+09:00" }
  ]
}

// paper.marks   — 밑줄 (5.7). 단어가 아니라 '책'에 매단다
{
  "ningen": [
    {
      text:      "그는 익살로 사람들을 웃겼지만, 그 웃음 밑에는 경멸이 깔려 있었다.",
      page:      null,
      wordIds:   ["ningen-1-익살", "ningen-1-경멸"],  // 담을 때 한 번만 계산
      createdAt: "2026-08-25T21:03:00+09:00"
    }
  ]
}

// paper.days   — 달력·스트릭용
{
  "2026-08-24": { words: 12, sentences: 2, book: "인간실격" }
}

// paper.settings
{ level: 2, dailyCount: null }   // dailyCount: null = 섹션에 맞춰 유동
```

---

## 9. 기술 스택

| 항목 | 선택 | 비고 |
|---|---|---|
| 프런트엔드 | HTML + CSS + 순수 JavaScript (ES 모듈) | 빌드 단계 없음 |
| 사전 API | 국립국어원 표준국어대사전 Open API | Vercel 서버리스 함수로 프록시 (인증키 은닉) |
| 느낌으로 찾기 | LLM API | 서버리스 함수로 프록시. 결과는 사전으로 재검증 |
| 학습 기록 | 브라우저 localStorage | 저장소 어댑터로 분리 (9.2) |
| 배포 | Vercel (GitHub 연동 자동 배포) | |

### 9.1 주의사항

- 사전 API의 `view.do`는 JSON을 지원하지 않는다. XML로 받아 파싱해야 한다
- `search.do`의 `num` 파라미터는 10 미만이면 빈 응답이 온다
- 표제어에 붙임표가 들어간다 ("아득-하다") — 제거 필요
- `hidden` 속성을 쓰는 요소에 CSS `display`를 지정하면 숨김이 무시된다.
  `[hidden] { display: none !important; }` 를 반드시 둔다
- 표준국어대사전 뜻풀이를 대량으로 저장·재배포하는 형태이므로,
  Open API 이용약관의 출처 표시·재배포 조건을 한 번 확인해둘 것 (현재 푸터 표기는 유지)

### 9.2 저장소 어댑터 — 로그인을 지우지 않는 방법

기존 기획의 1단계는 "로그인 제거"였는데, 이미 붙여둔 Supabase 코드를 지웠다가
9단계에서 다시 짜는 건 낭비다. 지우는 대신 **갈아 끼운다.**

```
lib/store.js            ← 인터페이스. 앱 코드는 여기만 부른다
lib/store.local.js      ← localStorage 구현 (지금 쓰는 것)
lib/store.supabase.js   ← 지금의 lib/db.js를 이름만 바꿔 옮김
config.js               ← STORE = "local" | "supabase"
```

- 앱 화면 코드는 `store.js`의 함수만 호출한다. 어느 쪽에 저장되는지 몰라야 한다
- 두 구현은 같은 함수 시그니처를 갖는다 (`getProgress`, `saveProgress`, `getSentences`, `addSentence`, `getDays` …)
- 9단계에서 할 일은 `config.js` 한 줄 바꾸기 + 로컬 데이터 업로드 마이그레이션뿐

---

## 10. 구현 순서

각 단계마다 실제 브라우저로 확인한 뒤 다음으로 넘어간다.
**한 번에 여러 단계를 진행하지 않는다.**

| 순서 | 내용 | 이유 |
|---|---|---|
| 1 | **저장소 어댑터 분리** — `store.js` + `store.local.js`, 로그인 화면 우회 | 흐름 확인이 막히지 않게. 기존 코드는 삭제하지 않는다 |
| 2 | 콘텐츠 형태 정비 — 고정 ID·`hanja`·`dictExample`·`level` 추가 | 뒤 단계 전부가 이 형태에 의존한다 |
| 3 | 학습 화면 (카드 넘기기) | 앱의 중심 |
| 4 | 퀴즈 — 뜻 고르기 4지선다 (오답 생성 규칙 포함) | 확인 수단 |
| 5 | 되새김 피드 + **한 줄 쓰기** | 반복 노출과 산출이 같이 사는 자리 |
| 6 | 망각곡선 복습 로직 (쓴 단어 간격 건너뛰기 포함) | 위 기능들에 자동 반영 |
| 7 | 홈 (되새김 · 책꽂이 · 달력) | 쌓인 게 있어야 의미가 생김 |
| 8 | 찾기 — 표제어 검색 + 느낌으로 찾기 | 단어를 모르는 사람의 입구 |
| 9 | 문장 노트 | 문장이 쌓인 뒤에 의미가 생김 |
| 10 | 콘텐츠 채우기 (섹션별·난이도별 어휘) | |
| 11 | 로그인 및 기기 간 동기화 — **문장부터** | 마지막 |

> 기존 순서와 달라진 곳: 1번(제거 → 어댑터), 2번(신설), 5번(한 줄 쓰기 결합), 8번(느낌으로 찾기 결합), 9번(신설).

---

## 11. 하지 않을 것

방향이 흐려지는 걸 막기 위해 명시한다.

- ❌ 단어 추가·삭제 버튼을 주요 화면에 노출하지 않는다 (찾기 화면 안으로)
- ❌ 뜻을 임의로 쉽게 풀어쓰지 않는다 (표준국어대사전 원문 유지)
- ❌ 번역본 문장을 그대로 인용하지 않는다 (저작권)
- ❌ 책 표지 이미지를 쓰지 않는다 (저작권)
- ❌ 타이핑 퀴즈를 기본으로 쓰지 않는다 (느리고 지침)
- ❌ 로그인을 초반에 요구하지 않는다
- ❌ 한 번에 오래 붙잡는 학습량을 강요하지 않는다
- ❌ **되새김 피드를 무한 스크롤로 만들지 않는다**
  "SNS 넘기듯이"는 조작감 얘기지 분량 얘기가 아니다.
  끝이 없으면 완료감이 없고, 이건 "짧게 여러 번" 원칙과 정면으로 충돌한다
- ❌ **내 문장을 채점·첨삭하지 않는다** (평가가 붙으면 안 쓴다)
- ❌ **문장 쓰기를 필수 단계로 만들지 않는다** (막히면 앱을 닫는다)
- ❌ 누적 단어 수를 경쟁 지표로 내세우지 않는다 (책꽂이가 그 역할을 한다)
- ❌ 시험 대비로 포지셔닝하지 않는다 (레드오션이고, 시험이 끝나면 통째로 이탈한다)

---

## 12. 확정 — 이전에 미결정이던 것

| 질문 | 결정 | 이유 |
|---|---|---|
| 책꽂이가 채워지는 단위 | **섹션마다** 얇은 책등 1개, 완독 시 두꺼운 한 권으로 합침 | 성취 간격이 짧아야 한다. 한 권은 너무 멀다 |
| 하루 분량 | **읽을 섹션에 맞춰 유동**, 상한 15개 | 1절 정의가 "읽기 전 준비"인데 고정 10개는 목적과 안 맞는다 |
| 되새김 피드 분량 | **하루 5~7장, 끝이 있음.** 그중 1~2장에 쓰기 칸 | 무한 스크롤은 자체 원칙 위반 (11절) |
| 인간실격 어휘 재선정 | **한다.** 단 10단계에서 | 2단계 기준으로 다시 뽑되, 골격이 선 다음 |

---

## 13. 아직 정하지 않은 것

- 느낌으로 찾기에 쓸 LLM과 호출 비용 상한 (무료 사용자에게 하루 몇 회까지?)
- 내가 쓴 문장이 앱 예문을 대체할 때, 사전 원문 예문은 어디에 남길지
- 일일 어휘 트랙을 계속 둘지 — 책 트랙이 자리 잡으면 존재 이유가 약해진다
- 두 번째 책을 무엇으로 할지 (인간실격 다음)
- 문장 공유 기능을 언젠가 열지 (지금은 명시적으로 안 함)
